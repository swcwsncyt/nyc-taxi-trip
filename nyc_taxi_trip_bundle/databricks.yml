# This is a Databricks asset bundle definition for nyc_taxi_trip_bundle.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: nyc_taxi_trip_bundle
  uuid: f68884dc-6c0e-4555-a7a5-74fe45427141

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-0ae7ca11-a203.cloud.databricks.com
    variables:
      catalog: dev
      schema: ${workspace.current_user.short_name}
  prod:
    mode: production
    workspace:
      host: https://dbc-0ae7ca11-a203.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/nataliechunws@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/nataliechunws@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: dev
      schema: prod
    permissions:
      - user_name: nataliechunws@gmail.com
        level: CAN_MANAGE
